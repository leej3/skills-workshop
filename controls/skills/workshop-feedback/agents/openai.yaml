interface:
  display_name: "Workshop Feedback"
  short_description: "Collect structured outcomes with private overlays"
  default_prompt: "Use $workshop-feedback for schema-validated skill outcomes; isolate sensitive fields, batch shareable records, and group exceptional lessons."
policy:
  allow_implicit_invocation: true
