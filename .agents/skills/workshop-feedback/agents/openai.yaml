interface:
  display_name: "Workshop Feedback"
  short_description: "Collect private outcomes and exceptional lessons"
  default_prompt: "Use $workshop-feedback for minimal local skill outcomes; group exceptional lessons and review publication separately."
policy:
  allow_implicit_invocation: true
